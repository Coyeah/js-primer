const {EXCHANGE_TYPE} = require('../constants/model');

// focus to exchange
module.exports = class CurrencyClass {
  constructor(params) {
    this.exchange = parmas.exchange
  }

  start() {
    console.log('ExchangesClass start');
  }
}
